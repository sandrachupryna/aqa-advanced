# Прототипне наслідування в JavaScript

Ця папка містить приклади прототипної моделі наслідування в JavaScript.

## Структура папки

### 📁 prototypes/
- **01-prototype-basics.js** - Основні концепції прототипів
- **02-prototype-chain.js** - Прототипний ланцюжок та пошук властивостей
- **03-classes-vs-prototypes.js** - Порівняння класів ES6 та прототипів

### 📁 test-automation/
- **prototype-in-test-automation.js** - Практичне застосування прототипів в автоматизації

## Основні концепції

### Прототипна модель
JavaScript використовує прототипну модель наслідування, а не класичну класову модель. Кожен об'єкт має посилання на інший об'єкт (прототип).

### Ключові поняття

#### prototype
Властивість функції-конструктора, яка містить методи та властивості, доступні всім екземплярам.

```javascript
function Person(name) {
    this.name = name;
}
Person.prototype.greet = function() {
    return `Привіт, я ${this.name}`;
};
```

#### __proto__
Посилання на прототип об'єкта (застаріле, але все ще використовується). Краще використовувати `Object.getPrototypeOf()`.

#### Прототипний ланцюжок
Механізм пошуку властивостей:
1. Спочатку шукає в самому об'єкті
2. Якщо не знайдено, шукає в прототипі
3. Продовжує до `Object.prototype`
4. Якщо не знайдено, повертає `undefined`

### Object.create()
Метод для створення об'єкта з вказаним прототипом.

```javascript
const prototype = { method: function() {} };
const obj = Object.create(prototype);
```

### Наслідування через прототипи

```javascript
// Базовий клас
function Animal(name) {
    this.name = name;
}

// Похідний клас
function Dog(name, breed) {
    Animal.call(this, name);
    this.breed = breed;
}

// Наслідування прототипу
Dog.prototype = Object.create(Animal.prototype);
Dog.prototype.constructor = Dog;
```

## Класи ES6 vs Прототипи

### Класи ES6
- Сучасний, читабельний синтаксис
- Синтаксичний цукор над прототипами
- Підтримка приватних полів (ES2022+)
- Підтримка статичних методів

### Прототипи
- Більше контролю та гнучкості
- Динамічна модифікація прототипів
- Сумісність зі старим кодом
- Розуміння основ JavaScript

**Важливо:** Класи ES6 під капотом працюють так само, як функції-конструктори з прототипами!

## Застосування в автоматизації тестування

### Розширення базових класів
Прототипи дозволяють розширювати функціональність існуючих класів без модифікації оригінального коду.

### Міксини
Додавання функціональності до класів через `Object.assign()`.

### Динамічне розширення
Додавання методів до прототипів після створення класу.

### Створення ієрархій
Прототипне наслідування дозволяє створювати гнучкі ієрархії класів для тестових фреймворків.

## Як використовувати

1. Відкрийте файл з прикладами
2. Прочитайте коментарі та пояснення
3. Зрозумійте, як працює прототипний ланцюжок
4. Експериментуйте з кодом
5. Порівняйте класи ES6 та прототипи

## Корисні методи

- `Object.getPrototypeOf(obj)` - отримання прототипу об'єкта
- `Object.create(proto)` - створення об'єкта з прототипом
- `Object.setPrototypeOf(obj, proto)` - встановлення прототипу
- `obj.hasOwnProperty(prop)` - перевірка власної властивості
- `prop in obj` - перевірка властивості в ланцюжку прототипів

