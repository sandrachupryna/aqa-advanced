const radius = 3.27;
const height = 10.5;
let volume = Math.PI * Math.pow(radius, 2) * height;

console.log(`The volume of a cylinder with radius ${radius} and height ${height} is: ${volume.toFixed(2)}`);